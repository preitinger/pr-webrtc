// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
    // TODO ? config if necessary
}

// const withPWA = require("@ducanh2912/next-pwa").default({
//     dest: "public",
// });

// module.exports = withPWA(nextConfig)

module.exports = nextConfig
